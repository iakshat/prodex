html
    head
        title Profile Pic
    body

        include ./header

        include ./navbar

        form(ref="uploadForm" id='uploadForm', 
        action="/upload_profile_pic/"+encryptedUsername,
        method = "POST",
        encType="multipart/form-data")

                input(type="file" name="sampleFile")

                input(type="submit", value='Upload!')


        include ./footer